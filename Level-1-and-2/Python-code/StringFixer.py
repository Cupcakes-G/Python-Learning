#Takes strings and...
    #1)if there are two spaces in a string, makes them one
    #2)adds space after period before letter of new sentence if there isn't one
def editor(str):
    for x in str:
        if x == " ":


